# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-spdx-diff"
BBFILE_PATTERN_meta-spdx-diff = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-spdx-diff = "6"

LAYERDEPENDS_meta-spdx-diff = "core"
LAYERSERIES_COMPAT_meta-spdx-diff = "scarthgap"

LAYERDIR_SBOM_SCRIPTS = "${LAYERDIR}"
